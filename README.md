# Operators Assignment

This is a simple C# console app that shows how operator overloading works.

- I created an Employee class with Id, FirstName, and LastName. 
- Then I overloaded the `==` and `!=` operators so two employees are compared based on their Id.
